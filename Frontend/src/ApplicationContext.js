import { createContext } from "react";

export const priorityFilters = createContext(null);
export const priorityFiltersDispatcher = createContext(null);

export const userInfo = createContext(null);
export const userInfoDispatcher = createContext(null); 